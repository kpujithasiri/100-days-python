DAY 8:CAESAR CIPHER
->All we  do mostly in our code is uding the function and calling the function as my function()all 
the time in our on going project
->in order to start with wwe have to call a function using def (func name) ()and then call it lastly after doing all the code with
function name.
eg:
def great():
    print("Hello sir")
    print("how do you do sir?")
    print("Isn't the weather nice?")

great()
->next is calling with name ntg but filling the paranthesis which we left empty before
eg:def greet_with_name(name):
    print(f"Hello {name}")
    print(f"how do you do {name}?")
    print(f"Isn't the weather nice {name}?")
greet_with_name("puji")
parameter is the name of the data that being passed in,argument is the actual value of data.
FUNCTION THAT ALLOWS MULTIPLE INPUTS:
->positional argument
def greet_with(name, location):
    print(f"Hello {name}")
    print(f"What is your experience of trip to {location}")

greet_with("puji", "paris")
# greet_with("paris", "puji")
->key word arguments
greet_with(name="puji",location="paris")
LOVE CALCULATOR:
def calculate_love_score(name1, name2):
    combined_names = name1 + name2
    lower_names = combined_names.lower()
    
    t = lower_names.count("t")
    r = lower_names.count("r")
    u = lower_names.count("u")
    e = lower_names.count("e")
    first_digit = t + r + u + e
    
    l = lower_names.count("l")
    o = lower_names.count("o")
    v = lower_names.count("v")
    e = lower_names.count("e")
    second_digit = l + o + v + e
    
    
    score = int(str(first_digit) + str(second_digit))
    print(score)
    
calculate_love_score("Kanye West", "Kim Kardashian")
