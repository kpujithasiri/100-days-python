DAY 10
------------FUNCTIONS WITH OUTPUTS--------
GOAL:To built a calculator app
1)The first step is all about converting strings to title case.
eg:
def format_name(f_name, l_name):
    print(f_name.title())
    print(l_name.title())


format_name("pujitha", "PUjitha")
return key word is imp in order to create a function that gives outputs.
usage of return function:
def function_1(text):
    return text + text

def function_2(text):
    return text.title()
output= function_2(function_1("hello"))
print(output)
2)Multiple return values
leap year checking code:
def is_leap_year(year):
    if year % 4 == 0:
        if year % 100 == 0:
            if year % 400 == 0:
                return True
            else:
                return False
        else:
            return True
    else:
        return False
3)Doc strings:
doc string is nothing but like comment lines telling wt would be the output expected it is placed very next to the func def line
eg:"""take a first name and last name and formatted"""

->first step towards cracking project is defining each any every operation of calculator.
->and go through the calling function and using loops in where ever wanted.